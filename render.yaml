services:
  - type: web
    name: sse-backend
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/sse-demo-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_OPTS
        value: -Xmx512m
